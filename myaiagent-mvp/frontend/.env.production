# Production Environment Configuration
# This file is used when building for production deployment

# API Configuration - Uses relative path for same-domain deployment
# The api.js will automatically detect werkules.com and use /api
VITE_API_URL=/api

# Enhanced Features
VITE_ENABLE_ENHANCED_STT=true
